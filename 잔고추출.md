하다가 중간에 멈췄는데 마저 구현해줘. 컴파일해서 adb로 설치해서 제대로 구현 되었는지 테스트까지 해야 되

지금까지 하던 일은 크게 세 가지입니다.
1. 퇴직연금 잔고 화면으로 정확히 들어가기
- 목표는 한국투자 MTS의 퇴직연금 ETF · 리츠 잔고 화면까지 자동 진입하는 것입니다.
- 7202 direct deep link 진입과, 실패 시 메뉴 기반 fallback 진입을 둘 다 다루고 있습니다.
- 구현 파일:
  - /Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/NeoSmartAccessibilityService.kt
    - navigateToRetirementBalanceScreen()
    - navigateToRetirementBalanceDirectScreen()
    - isRetirementBalanceScreen()
    - hasRetirementListMarkers()
2. 잔고 그리드에서 종목명 / 매도가능 / 매입단가 / ticker 추출하기
- IRP / DC 계좌 각각에 대해 잔고 리스트를 읽어서
  - 종목명
  - 매도가능
  - 매입단가
  - ticker
  를 뽑아 로그/앱 화면에 보내는 작업입니다.
- 최근에는 잘못된 화면 텍스트를 섞어 읽지 않도록
  - 전체 화면 값 fallback을 줄이고
  - 그리드 영역만 보도록 제한하고
  - 가로 스와이프 상태별로 부분 추출을 합치는 방향으로 보강했습니다.
- 구현 파일:
  - /Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/NeoSmartAccessibilityService.kt
    - getBalanceFrom7201()
    - extractRetirementHoldingSnapshot()
    - extractListSnapshot()
    - collectRetirementGridBoxes()
    - resolveBestGridRow()
    - findGridValueAt()
    - extractGridColumnFallback()
    - discoverTickerForHolding()
    - extractTickerFromTopArea()
3. 메인 앱 버튼/설정/결과 표시 구성
- 앱 메인 화면에서
  - [퇴직연금 주문]
  - [퇴직연금 잔고]
  버튼으로 명령을 보내고,
  결과를 화면에 표시하도록 구성했습니다.
- 종목명 -> ticker 저장도 준비되어 있습니다.
- 구현 파일:
  - /Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/MainActivity.kt
    - 버튼 클릭 시 GET_BALANCE, 주문 화면 이동 브로드캐스트 전송
  - /Users/y/Github/yquant-mtsa/app/src/main/res/layout/activity_main.xml
    - 메인 버튼 UI
  - /Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/ConfigManager.kt
    - loadHoldingTickerMap()
    - saveHoldingTicker()
현재 상태를 한 줄로 말하면:
- 잔고 화면 자체(7202)가 어떤 레이아웃인지 파악했고,
- 그 레이아웃 기준으로 파서/화면 판별 로직을 고치는 중이며,
- 아직 최종적으로는 “항상 안정적으로 화면 진입 + 정확한 값 추출”까지는 완료되지 않은 상태입니다.