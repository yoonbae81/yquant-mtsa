4x3으로 1234567890 키가 배치되고 (2개의 공백이 랜덤하게 배치), 그 하단에는 백스페이스와 입력완료가 있으니까, 내 아이디어는 숫자키가 배치될 수 있는  12개의 공간을 좌측 상단부터  한번씩 눌러보고, 키가 입력 됐으면 그 자리는 숫자 1인거고, 백스페이스키를 눌러 입력된 숫자를 지우는거지. 그리고 2가 있을 다음 칸을 눌러봤는데 입력된게 없다면 빈칸 2개를 찾은거지. 그레서 세번째 칸을 눌렀을때 입력된게 있다면 2는 3번째 칸에 있는거고, 다시 백스페이스를 누르는거지. 이런식으로 OCR 없이도 숫자판을 알아낼 수 있어. 

숫자판 누르는 방법
1. 비밀번호 팝업에서 접근성 노드를 전부 수집합니다.
2. 그중 텍스트가 0~9 한 글자인 노드만 골라서 digit -> node 매핑을 만듭니다.
3. 비밀번호 첫 자리 숫자(예: 7)에 해당하는 노드를 찾습니다.
4. 그 노드에 대해 AccessibilityNodeInfo.ACTION_CLICK을 직접 호출합니다.
5. 클릭 후 점 표시 변화가 있는지 확인합니다. 지금 코드는 et_password.text.length가 없으면 ll_password/keylayout의 child count 변화로 fallback 감지를 하도록 바뀌어 있습니다.
6. 테스트 모드에서는 안전하게 한 자리만 누르고 멈추거나, 성공 시 바로 backspace를 눌러 되돌리게 했습니다.
즉 핵심은 좌표 추정보다 먼저, 숫자 라벨이 달린 접근성 노드 자체를 클릭해보는 방식입니다.  
다른 세션에서 시작할 때는 NeoSmartAccessibilityService.kt에서 collectAccountPopupDigitNodes(), tryAccountPopupDigitStrategies()의 strategy 1, 그리고 testAccountPasswordProbe()를 보면 됩니다.


숫자판 누르는 코드
/Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/NeoSmartAccessibilityService.kt 입니다.
전략 1 관련 핵심 함수는 이 파일 안의:
- collectAccountPopupDigitNodes()
- tryAccountPopupDigitStrategies()의 첫 번째 전략
- enterAccountPasswordByDigitNodes()
- testAccountPasswordProbe()
입니다.
▣


숫자 비밀번호 팝업을 띄우는 현재 구현 코드는  
/Users/y/Github/yquant-mtsa/app/src/main/java/com/yquant/mtsa/NeoSmartAccessibilityService.kt  
에 있습니다.
핵심 함수는 두 개입니다.
1. openAccountPasswordPopupForCurrentOrder()
- 전용 명령 OPEN_ACCOUNT_PASSWORD_POPUP을 받아
- 7201 주문 화면으로 이동하고
- 사용자 탭을 맞춘 뒤
- 우측 [비밀번호] 버튼을 눌러
- 팝업이 실제로 떴는지 확인합니다.
2. openAccountPasswordKeyboard()
- 이미 계좌 비밀번호 입력 팝업이 떠 있는 상태에서
- 키패드가 열려 있는지 확인하고
- 아니면 et_password / tf_password / 비밀번호 노드를 찾아서
- 키패드를 열려고 시도합니다.
또 이 전용 경로를 타는 명령 분기는 같은 파일의 handleCommand() 안에 있고, 명령 상수 이름은 CMD_OPEN_ACCOUNT_PASSWORD_POPUP 입니다.